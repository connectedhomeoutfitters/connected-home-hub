<%# Compact header for inner pages. The home page has its own full hero instead. %>
<div class="field field--fade">
  <section class="pagehead">
    <div class="wrap">
      <p class="eyebrow"><%= eyebrow %></p>
      <h1><%= heading %></h1>
      <p class="lede pagehead__lede"><%= lede %></p>
    </div>
  </section>
</div>
